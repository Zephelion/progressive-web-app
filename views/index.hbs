<main>
    <ul>
        {{#each paintings}}
        <li class="loading">
            <a href="{{objectNumber}}">
                <img src="{{smallImg}}" alt="{{title}}">
            </a>
        </li>
        {{/each}}
    </ul>
</main>